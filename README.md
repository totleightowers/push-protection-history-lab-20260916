# Push protection history experiment

This public repository contains only synthetic test data. No personal information or working credentials are used.

The experiment tests whether a custom secret-scanning pattern prevents an old clone from restoring history after a repository is cleaned. All commits are signed. No bypass of secret-scanning push protection will be used.
